class Solution {
  public:
    bool areRotations(string &s1, string &s2) {
        // code here
        if(s1.size()!=s2.size()){
            return false;
        }
        
        string temp=s1+s1;
        if(temp.find(s2)!=string::npos)
        return true;
        else
        return false;
        
    }
};
